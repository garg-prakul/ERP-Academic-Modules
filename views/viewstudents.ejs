<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>View Students</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <div class="header">
    <h1>Student Management System</h1>
    <div class="nav">
      <a href="/viewstudents" class="nav-link">Students</a>
      <a href="/" class="nav-link">Home</a>
    </div>
  </div>

  <div class="container">
    <h2>All Students</h2>
    <p>Displaying <%= students.length %> student records</p>
    
    <table class="data-table">
      <thead>
        <tr>
          <th>Roll No</th>
          <th>Name</th>
          <th>Email</th>
        </tr>
      </thead>
      <tbody>
        <% students.forEach(student => { %>
          <tr>
            <td><%= student.roll_no %></td>
            <td><%= student.student_name %></td>
            <td><%= student.email %></td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  </div>

  <div class="footer">
    &copy; <%= new Date().getFullYear() %> Student Management System
  </div>
</body>
</html>